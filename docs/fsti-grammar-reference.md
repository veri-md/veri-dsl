# F* Grammar Reference (Internal)

> This is an internal tooling reference. For Veri DSL syntax (the user-facing DSL),
> see `dsl-grammar-v0.2.md` and `veri-fsti-correspondence.md`.

This file documents the F* subset understood by the F* parser (`fsti_parser.py`).
It is a tooling implementation detail, not user-facing documentation.

For Veri DSL → F* mapping, see `veri-fsti-correspondence.md`.
For the full Veri DSL grammar, see `dsl-grammar-v0.2.md`.
